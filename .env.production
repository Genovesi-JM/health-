# ═══════════════════════════════════════════════════════════════
# Health Platform — Production Environment Variables
# DigitalOcean Droplet Deployment
#
# INSTRUCTIONS:
#   1. Copy this file to .env in the project root:
#      cp .env.production .env
#   2. Fill in ALL values marked with CHANGE_ME
#   3. Never commit the .env file to git!
# ═══════════════════════════════════════════════════════════════

# ── App Environment ──
ENV=production

# ── Security (REQUIRED — generate with: openssl rand -base64 48) ──
SECRET_KEY=CHANGE_ME_GENERATE_WITH_OPENSSL

# ── Database ──
DB_PASSWORD=CHANGE_ME_STRONG_DB_PASSWORD

# ── Admin Seed Account ──
ADMIN_PASSWORD=CHANGE_ME_ADMIN_PASSWORD

# ── URLs (update with your domain or Droplet IP) ──
# If using a domain: https://health.yourdomain.com
# If using IP only:  http://YOUR_DROPLET_IP
FRONTEND_BASE=http://YOUR_DROPLET_IP
BACKEND_BASE=http://YOUR_DROPLET_IP
CORS_ORIGINS=http://YOUR_DROPLET_IP,https://genovesi-jm.github.io

# ── Port (80 for HTTP, 443 if using SSL) ──
APP_PORT=80

# ── DigitalOcean Spaces (optional — for file storage) ──
DO_SPACES_KEY=
DO_SPACES_SECRET=
DO_SPACES_BUCKET=health-platform-files
DO_SPACES_REGION=fra1

# ── OpenAI (optional — for AI-powered chatbot) ──
OPENAI_API_KEY=

# ── Stripe (optional — for payments) ──
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
