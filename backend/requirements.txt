fastapi==0.111.0
uvicorn[standard]==0.30.0
SQLAlchemy==2.0.30
bcrypt==4.0.1
passlib[bcrypt]==1.7.4
python-jose==3.3.0
PyJWT
python-multipart==0.0.9
pydantic==2.7.1
pydantic-settings==2.2.1
httpx

# S3-compatible storage (DigitalOcean Spaces)
boto3>=1.34.0

# Encryption for secrets at rest
cryptography>=42.0.0

# Validation helpers
email-validator

# Database migrations
alembic>=1.13.0

# PDF generation (prescriptions & referrals)
reportlab>=4.0.0

# Redis (session/queue — optional for MVP)
redis>=5.0.0

# Testing
pytest>=8.0.0
pytest-cov>=5.0.0

# Production ASGI server
gunicorn>=22.0.0
