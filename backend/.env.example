# ═══════════════════════════════════════════════════════════════
# Health Platform — Environment Variables
# Copy to `backend/.env` for local dev OR set in DigitalOcean App Platform.
# IMPORTANT: do NOT commit real values.
# ═══════════════════════════════════════════════════════════════

# ── Core ──
SECRET_KEY=CHANGE_ME_TO_A_LONG_RANDOM_SECRET
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# ── Database ──
# SQLite (dev):   sqlite:///./health_platform.db
# PostgreSQL:     postgresql://health_user:password@db:5432/health_platform
DATABASE_URL=sqlite:///./health_platform.db

# ── CORS (comma-separated origins) ──
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ── DigitalOcean Spaces (S3-compatible storage for prescriptions/referrals) ──
DO_SPACES_KEY=
DO_SPACES_SECRET=
DO_SPACES_BUCKET=health-platform-files
DO_SPACES_REGION=nyc3
DO_SPACES_ENDPOINT=https://nyc3.digitaloceanspaces.com

# ── Stripe (payments) ──
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ── Redis (sessions / rate-limiting / queues) ──
REDIS_URL=redis://localhost:6379/0

# ── Docker Compose DB password ──
DB_PASSWORD=changeme

# ── Google OAuth (optional — kept from existing auth) ──
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:8000/auth/google/callback

# ── Microsoft OAuth (optional) ──
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
MICROSOFT_REDIRECT_URI=http://localhost:8000/auth/microsoft/callback
MICROSOFT_TENANT=common

# ── Email / SMTP (password resets) ──
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=no-reply@healthplatform.com
# SMTP_PASSWORD=your_password
# SMTP_FROM=no-reply@healthplatform.com
# SMTP_USE_TLS=true
