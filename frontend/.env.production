# Production API URL — DigitalOcean deployment
# When running in Docker on the Droplet, nginx proxies /api/ → backend container,
# so VITE_API_URL should be empty (uses relative paths).
# When accessing via GitHub Pages → DO backend, set to https://health.geovisionops.com
VITE_API_URL=https://health.geovisionops.com
