# Production API URL — update this after deploying the backend to Render
# The exact URL will be shown in the Render dashboard after first deploy
VITE_API_URL=https://health-api-latest.onrender.com
